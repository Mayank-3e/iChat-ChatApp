1. Install dependencies in `root` and `api` folders.
2. `npm run ngrok` in `api` then copy its link to `config.js`
3. `npm start` to serve client files and can be accessed through local network.

![](./Screenshot1.png)

![](./Screenshot2.jpg)